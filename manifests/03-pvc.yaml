apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openclaw-state
  labels:
    app: openclaw
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp3-csi
  resources:
    requests:
      storage: 5Gi
